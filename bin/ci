#!/usr/bin/env bash
# Zero-cost regression checks on this repo's own rules text (harness-rules.md,
# CLAUDE.md, and the issue-to-pr / next-to-build prompts).
# No model calls.
set -euo pipefail
cd "$(dirname "$0")/.."

exec ./bin/check-rules
