## This repo

- This repository *is* the harness, so `.claude/harness-rules.md` is the source here rather than a rules to follow.
- The test suite is `bin/ci` (zero-cost checks on this repo's own rules text — no model calls). Run it before opening a PR.
